# ERP V2 — Controlled Production Enablement Plan: Voucher Generate / Preview / Download

**Date:** 2026-07-17
**Status:** Planning only. **No flags changed, no production/staging touched, no voucher generated.** No
code, schema, environment, or data change accompanies this plan.

## 1. Purpose
- Controlled production enablement plan for **single-service voucher generate / preview / download**.
- The next V2 production surface after Ops supplier assignment / confirmation.
- **Planning only.**
- **No voucher generated.**
- **No flags changed.**

## 2. Read-only findings
- **Generate** uses **`NEXT_PUBLIC_OPS_V2_VOUCHER_GENERATE`**.
- **Preview** uses **`NEXT_PUBLIC_OPS_V2_VOUCHER_PREVIEW`**.
- **Download / PDF** uses **`NEXT_PUBLIC_OPS_V2_VOUCHER_DOWNLOAD`**.
- **Backend routes are role-gated with no backend flag** — `POST :id/operations/:operationId/voucher/generate`,
  `GET :id/operations/:operationId/voucher`, `GET :id/operations/:operationId/voucher/pdf`, all
  `admin` / `operations`; the download route is a *pure render — no mutation / status / send / audit /
  email / finance*.
- **Send-preview and send are out of scope.**
- **Voucher-send remains separately disabled** by `OPS_V2_VOUCHER_SEND_ENABLED=false`.

## 3. Current flag state
- **Preview appears OFF / "Coming later".**
- **Download appears OFF / "Coming later".**
- **Send-preview and Send appear OFF / "Coming later".**
- **Generate value needs a fresh runtime probe** (it did not render "Coming later", but that is ambiguous —
  it may be the live control's ineligible state).
- All five voucher flags are **present in `-4gu9`** but their baked values are **masked** to the CLI.
- **Exact baked values must be confirmed (runtime probe) before GO.**

## 4. Scope to enable
- Generate **one** single-service voucher.
- **Preview** voucher.
- **Download / render** voucher PDF.
- **Duplicate / regenerate guard.**
- **No send.**

## 5. Explicitly out of scope
- voucher-send.
- packet generation.
- packet-send.
- supplier email.
- passenger / rooming edits.
- supplier assignment changes.
- booking creation.
- quote edits.
- finance writes.

## 6. Required production flags
- Enable only **`NEXT_PUBLIC_OPS_V2_VOUCHER_GENERATE`**, **`NEXT_PUBLIC_OPS_V2_VOUCHER_PREVIEW`**,
  **`NEXT_PUBLIC_OPS_V2_VOUCHER_DOWNLOAD`** **if the runtime probe shows them OFF**.
- `NEXT_PUBLIC` flags require a **`-4gu9` rebuild if changed**.
- **No backend flag exists** for generate / preview / download.
- **Send flags remain OFF.**
- **Do not change any other flag.**

## 7. Test-record strategy
- Use **`BK-2026-0007`**.
- Booking has **one assigned + confirmed service**.
- Supplier is **`ZZZ TEST SUPPLIER — DO NOT SEND`**.
- Service **has no voucher yet**.
- **No real client / supplier communication.**
- **No packet.**
- **No email.**

## 8. Smoke-test plan
- Generate one voucher.
- Confirm voucher status **GENERATED**.
- **Preview renders.**
- **PDF renders.**
- **PDF is finance-safe** — no cost / margin / payable leak.
- **Duplicate / regenerate guard does not create a second voucher.**
- **Booking status / totals / currency unchanged.**
- **No email.**
- **No packet.**

## 9. Role access
- **admin / operations** — allowed.
- **super_admin** — allowed via guard coalescing.
- **finance / agent / viewer** — blocked.
- **PDF route admin / operations only.**

## 10. Rollback plan
- Turn the enabled voucher frontend flags **OFF**.
- **Rebuild `-4gu9`.**
- Confirm the controls return to "Coming later".
- **Do not delete the generated voucher** unless separately approved.

## 11. Monitoring
- API / admin-web errors.
- Voucher count.
- PDF content safety.
- No margin / cost leak.
- No email / send events.
- Booking totals unchanged.

## 12. GO / NO-GO
- **GO** only if the exact baked flag values are confirmed.
- **GO** only if `BK-2026-0007` remains voucher-free and suitable.
- **GO** only if send remains disabled.
- **GO** only with a rollback owner.
- **NO-GO** on send ambiguity or PDF finance-safety uncertainty.

## 13. Recommended execution order
1. Approve plan.
2. Runtime-probe the voucher flags on `-4gu9`.
3. Enable only the OFF generate / preview / download flags if needed.
4. Rebuild `-4gu9` only if changed.
5. Run one smoke.
6. Document result.
7. Keep packet / send paused.

## 14. Safety boundaries
- Voucher-send allowlist remains **`ziad@axisdmc.com` only**.
- **Supplier sending remains disabled.**
- No supplier emails.
- No voucher-send.
- No packet-send.

## 15. Net conclusion
- Voucher generate / preview / download is **near-ready**.
- **`BK-2026-0007`** is the preferred test record.
- Execution is **pending the runtime flag probe and explicit GO**.
- **No flags changed and no voucher generated by this plan.**

## Confirmations
- No code changed.
- No data changed.
- No flags / environment changed.
- No production / staging behavior changed.
- No voucher generated.
- No packet created.
- No email sent.
- No supplier-send or voucher-send action.
- Voucher-send allowlist remains `ziad@axisdmc.com` only.
- Supplier sending remains disabled.

### Safety confirmations
- Documentation only — no code, schema, flag, environment, or data change accompanies this plan.
- No secrets, passwords, DB URLs, credentials, internal hosts, raw deployment URLs, project identifiers,
  scratch links, session tokens, cookies, or internal UUIDs / raw user / supplier / invoice / voucher /
  packet IDs are recorded here — only the human-readable booking reference, supplier label, flag / role
  names, and the plan.
